[profile.default]
src = "contracts"
out = "out"
libs = ["lib"]
# Uniswap v4 is consumed from the pinned git submodules recorded by the
# repository gitlinks and foundry.lock, not from npm packages.
solc = "0.8.26"
evm_version = "cancun"
optimizer = true
optimizer_runs = 200
via_ir = true
